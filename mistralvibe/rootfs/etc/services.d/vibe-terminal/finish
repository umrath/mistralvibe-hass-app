#!/usr/bin/with-contenv bashio
pkill -f "ttyd --port 7682" 2>/dev/null
pkill -f "ttyd --port 7683" 2>/dev/null
if [ "${1}" -ne 0 ] && [ "${1}" -ne 256 ]; then
    bashio::log.warning "vibe-terminal exited with error code ${1}"
fi
